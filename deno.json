{
  "tasks": {
    "build": "deno run --allow-read --allow-write build.ts",
    "serve": "deno task build && deno run --allow-net --allow-read jsr:@std/http@1/file-server _site/",
    "watch": "deno run --allow-read --allow-write --watch=content/ build.ts"
  }
}
